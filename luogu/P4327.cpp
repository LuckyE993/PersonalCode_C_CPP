#include "iostream"
#include "string"
#include "cstring"
using namespace std;
char source[15];
char c[200][200];
int main(int argc, char* argv[])
{
    cin>>source;
    int len = strlen(source);
    for (int i=0; i<len; i++)
    {
        if (i%2==0)
        {
            c[i][0] = '#';
        }
        if()

    }
    return 0;
}